{
  "name": "npm",
  "version": "1.0.0",
  "description": "Basic todo list for keeping track of tasks",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "PGPASSWORD=goodworks17 psql -U postgres -d postgres -a -f ../npm/todo.sql"
  },
  "author": "Zakai Meghoo-Peddie",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
